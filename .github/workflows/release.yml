name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Update plugin version
        run: node developer/update-plugin-version.js

      - name: Commit updated files
        run: |
          git config --local user.name "Nocs Bot 🤖"
          git config --local user.email "info@nocslab.ifrn.edu.br"
          git add .
          git commit -m "chore: update plugin version to ${GITHUB_REF_NAME}"
          git checkout -b update-plugin-version-${GITHUB_REF_NAME}
          git push origin update-plugin-version-${GITHUB_REF_NAME}

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: update-plugin-version-${GITHUB_REF_NAME}
          title: "chore: update plugin version to ${GITHUB_REF_NAME}"
          body: "Automated PR to update the plugin version to ${GITHUB_REF_NAME}"
          base: main

      - name: Create plugin ZIP
        run: node developer/create-zip.js

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: dist/obatala.zip
          asset_name: obatala.zip
          asset_content_type: application/zip
